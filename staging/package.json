{
  "name": "${project.artifactId}",
  "version": "${version.unrevise}",
  "description": "${project.description}",
  "scripts": {
    "postinstall": "gulp"
  },
  "dependencies": {
    "ace-builds": "${version.unrevise}"
  },
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-rename": "^2.0.0"
  }
}
